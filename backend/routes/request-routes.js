const routes = require("express").Router(); 

/* User models */
// const userModel = require('');

routes.post('/peopleFromLocation', (req, res) => {

    // req.body is the data passed in
    // res.send({ <DATA HERE >})

})


module.exports = routes;